# Good-to-haves

Deferred work items that are not part of the MVP but are worth picking up later. Each entry should describe *what* the feature is, *why* it was deferred, and *what implementing it entails*.

<!-- Item "FSR explicit disable-path (rotation Step 3 & 4)" was shipped. The
     enabling/enabled -> disabling -> disabled rotation + idempotent
     DisableFastSnapshotRestores call is live in pkg/snapshots/fsr.go
     (reconcileDisable) and pkg/fsr/{aws,fake}.go. -->
